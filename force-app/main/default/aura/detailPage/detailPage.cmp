<aura:component implements="flexipage:availableForAllPageTypes" access="global">
    <aura:attribute name="contact" type="Object"/>
        <span>
            <a href="" aura:id="{!v.contact.AccountId}" onclick="{!c.navigateToDetail}">{!v.contact.Account.Name}</a>
        </span>
</aura:component>	
